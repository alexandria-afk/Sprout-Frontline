// Scaffold test — verifies the app structure compiles and the
// FrontlineApp widget can be pumped. Full integration tests added in Phase 2.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — add tests once Supabase is configured', () {
    expect(true, isTrue);
  });
}
